# .gitlab-ci.yml - STARTER VERSION

stages:
  - test

test:
  stage: test
  image: python:3.11
  before_script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - pytest tests/ -v
  only:
    - branches
    - merge_requests