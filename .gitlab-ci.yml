stages:
  - test

test:
  stage: test
  image: python:3.11
  tags:
    - docker
  before_script:
    - pip install --upgrade pip
    - pip install -r backend/requirements.txt  
  script:
    - pytest backend/tests/ -v  
  only:
    - branches
    - merge_requests